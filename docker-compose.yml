services:
  kiro-rs:
    image: ${KIRO_RS_IMAGE:-ghcr.io/daniellee2015/kiro-rs-ultra:latest}
    container_name: kiro-rs
    extra_hosts:
      - "host.docker.internal:host-gateway"
    ports:
      - "8990:8990"
    volumes:
      - ./data/:/app/config/
    restart: unless-stopped
